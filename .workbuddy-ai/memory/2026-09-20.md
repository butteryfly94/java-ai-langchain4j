# 2026-09-20

## 通读 ai-langchain4j-prd 项目并输出改进建议

范围：`java-ai-langchain4j`（Spring Boot 3.4.7 + langchain4j 1.20.0/beta30 的医院智能客服 RAG）、
`xiaozhi-ui`（Vue3 + Vite 前端）、`docker-compose.yml`（ES 8.14.3）。

### 项目现状（已确认）
- 两条对话链路共用一个入口 `POST /api/xiaozhi/chat`：`QueryComplexityRouter` 按复杂度路由到
  固定 RAG（`retrievalAugmentorXiaozhi`）或 Agentic RAG（工具 + MCP filesystem）。
- 检索链路：查询扩展 → kNN/BM25 双路 → RRF 融合 → 父块扩展 → gte-rerank，`RrfRerankAggregator` 自定义实现。
- 摄入链路：父子分块（1000/300），MinerU `_content_list.json` 走 `TableAwareSplitter` 表格感知切分；
  `IngestionStats.warnings` 作为静默失效闸门。
- 引用溯源：`CitationRecorder` + 流末尾 `---SOURCES---` 标记。
- 记忆：MongoDB `MongoChatMemoryStore`，两个助手共用 `chatMemoryProviderXiaozhi`（maxMessages=20）。

### 已提出的主要改进点
1. **低风险快修**：`rag.router.enabled=false` 时 `ChatController` 仍会注入 `QueryComplexityRouter`（被无条件创建的 `@Service`），
   会连带创建 `routerChatModel`，导致"关掉路由"并不能省掉那条链路；建议给 router 加 `@ConditionalOnProperty`。
2. **路由不看对话历史**：`QueryComplexityRouter` 是纯函数，`"那第二个呢？"` 这类指代追问必判错，且 `intent` 不随 `memoryId` 传递；
   建议把最近一轮 user message 拼进判定输入。
3. **MCP filesystem 指向 knowledge/**：写文件工具与索引目录同源，LLM 可篡改知识源；建议副本目录 + 只读白名单。
4. **查询扩展放大 rerank 成本**：`ExpandingQueryTransformer` 为每个变体各跑一路双检索，
   但 rerank 仍只对"用户原问题 vs 父块"打分（`resolveOriginalQuery`），变体收益与成本不匹配。
5. **混合检索退化为串行**：`HybridRetrievalService.retrieveFromBoth` 顺序调用 kNN/BM25 并注释说交给调用方并行，
   实际无人并行化；建议用虚拟线程/`CompletableFuture` 并行。
6. **IngestionController 无并发保护**：`ingestAll()` 先删后建索引，两个请求并发会删掉对方正在写的索引，
   摄入期间 `/chat` 也会命中空索引；建议加锁 + 重建后原子切换（别名切换）。
7. **引用与答案不同源**：固定链路把所有 rerank 父块登记为引用（含 LLM 实际未使用的）；
   agentic 链路登记 RRF 全量（`KnowledgeSearchTool` 未过滤）。建议只登记真正注入 prompt 的部分。
8. **测试基建**：14 个测试全是 `@SpringBootTest` 真连 ES/Mongo/MySQL/LLM，无 CI 可跑的纯单测；
   `IngestionController` 无鉴权即可重建索引（DoS 面）。
9. **工程卫生**：仓库根目录混入 `马亚平.docx`、`~$马亚平.docx`、`resume_render_after/`、`optimize_resume.py` 等无关文件；
   `java-ai-langchain4j/hs_err_pid16816.log`（JVM 崩溃日志）应删除。
10. **存在与注释矛盾的过时类**：`Assistant`/`MemoryChatAssistant`/`SeparateChatAssistant` 及其 Config 是教程遗留，
    但注释声称"注解式装配拿不到控制权"已被 langchain4j 1.20 支持（`SeparateChatAssistant` 仍在用 `@AiService`）；
    建议或删除、或补 `@Profile` 隔离为 demo。
11. **前端**：`convertStreamOutput` 对 `&<>` 做转义**晚于**其他地方；`onDownloadProgress` 每帧全量重解析 + 转义，
    长回答浪费；`v-html` 与来源卡片渲染均可行但建议改 Markdown 渲染。
12. **pdf2md.py 路径**：脚本在 `java-ai-langchain4j/tools/`，但 `PROJECT_DIR` 取的是 parent.parent，
    实际成 `java-ai-langchain4j/`；`application.properties` 中 `rag.knowledge-dir=knowledge` 是相对 CWD，
    从仓库根启动会指向错误的目录。

### 结论
项目整体设计质量高于常见 Demo：注释记录设计取舍、"静默失效显式化"（摄入 warnings）、
链路对称性（两条链路共用同一批检索 bean）是几个明显优于同类的点。
待办优先级建议：先修 1/6/2/3（正确性与安全），再处理 4/5（成本与延迟），最后做 9/10/11（工程卫生）。

---

## 持续工作：实现了增量索引更新 + 别名原子发布

对应上面改进点清单的第 2、6 条（摄入并发保护、先删后建的不可用窗口）。

### 构建环境（踩过的坑，下次直接复用）
- **Maven 必须显式指定 JDK17**：`PATH` 里的 `java` 是 JRE 1.8（`C:\Program Files\Java\jre1.8.0_351`），
  `mvn` 会报 "No compiler is provided" 或 "JAVA_HOME not defined correctly"。
  可用组合（`bin/mvn` 的 shell 脚本坏了，必须用 `mvn.cmd`）：
  ```
  export JAVA_HOME='C:\Program Files\Java\jdk-17.0.9'   # 必须Windows风格路径
  export PATH="/d/apache-maven-3.9.6/bin:$PATH"
  mvn.cmd -o compile
  ```
- **ES client 实际解析到 8.15.5**（pom 里 `langchain4j-elasticsearch` 声明 9.3.1，但实际生效是 8.15.5）。
  用 javap 查 API 的方法：
  `jar xf C:/Users/73519/.m2/repository/co/elastic/clients/elasticsearch-java/8.15.5/elasticsearch-java-8.15.5.jar <class>`
- **`EmbeddingStore` 没有「按索引名写入」的重载**（只有 `addAll(List<Embedding>, List<Embedded>)`）。
  要写到指定索引必须新建一个绑定该索引名的 store（`ElasticsearchEmbeddingStore.builder().indexName(...)`）。
- 构建产物日志里有 `\r`，grep 要加 `-a`。

### 新增文件
- `rag/ingestion/IndexManager.java` —— 索引/别名生命周期。核心是 `beginPublication()`
  （建新物理索引，返回 `IndexPublication` 实现 `AutoCloseable`）→ 写入 → `publish()`
  （refresh + 原子切别名）。未 publish 就 close 会清理半成品且不影响线上。
  处理了**「同名普通索引」遗留状态**（ES 里索引名与别名不能同名，不处理会直接失败）。
- `rag/ingestion/DocumentFingerprintStore.java` —— 指纹账本（`knowledge-documents` 索引）。
  `hashOf()` 用 SHA-256 + **显式 UTF-8**（无参 getBytes 会导致跨平台 hash 不一致）。
  `configFingerprint()` 把切分参数拼起来做 hash —— **这条是关键**：
  切分参数变了但文档没变时，必须强制重灌，否则索引里还是旧切分结果且不报错。
- `src/test/java/.../DocumentFingerprintTest.java` —— 7 个纯单测（无 Spring），覆盖
  "未变跳过/变了重灌/新增重灌/**配置变了强制重灌**/配置指纹敏感性/hash稳定性/UTF-8"。

### 改动文件
- `KnowledgeIngestionService.java` —— **重写**。入口变为 `ingest(force)`（默认增量）+
  `ingestAll()`（= `ingest(true)`，保留旧语义但走别名发布）。流程：
  盘点文档算 hash → 与账本比对 → 建新索引 → `carryOverUnchanged()` 用 ES `_reindex`
  **把未变文档的块直接搬运（不产生 embedding 调用）** → 只重灌变更文档 → 原子切换 →
  更新账本（**必须在发布之后**，否则发布失败会导致账本超前、下次跳过该批）。
  加了 `ReentrantLock` + `tryLock` 防并发摄入。
- `IngestionStats.java` —— 重写。加 `label()`（非计数描述字段，如 mode）、
  按文档的父子块计数（供账本记录）、`put()` 覆盖式设值。原 `put(String,Integer)` 语义保留。
- `IngestionController.java` —— 加 `?force=` 参数，并发冲突返回 **409**（不是 200，
  否则调用方以为成功而实际什么都没做）。
- `application.properties` —— 新增 4 项：`rag.index.documents`、
  `rag.ingestion.incremental`、`rag.ingestion.keep-old-indices`、`rag.ingestion.prune-deleted`。
- `pdf-source/README.md` —— 补了增量用法、stats 字段读法、**别名回滚运维手册**。

### 关键设计取舍（面试/复盘时值得讲）
- **增量时也建全新索引，而不是原地改旧索引**。代价是未变文档的块要搬一遍，
  换来任何失败都能整体回滚（别名始终指向完整旧索引）。搬运走 `_reindex` 不产生 embedding，
  而 embedding 才是唯一真金白银开销 —— 所以这个取舍几乎免费。
- **指纹账本记 contentHash + configFingerprint 两个**。只记前者会导致"改了切分参数
  但增量跳过重灌"的静默失效。

### 验证状态（重要）
- ✅ `mvn -o compile` 通过
- ✅ `DocumentFingerprintTest`(7) + `TableAwareSplitterTest`(12) + `CitationRecorderTest`(9) = **28 个测试全通过**
- ❌ **端到端未验证**：本机 ES / MongoDB / DASH_SCOPE_API_KEY 均不可用
  （docker 不可用，ES 端口拒绝连接）。别名切换、`_reindex` 搬运、
  以及最关键的一条 —— **langchain4j 自动创建的 children 索引里 docId 字段类型**
  （若是 text 而非 keyword，`must_not + terms` 排除可能不精确）—— 都还没在真实 ES 上验证过。
  跑通验证的前置：`docker compose up -d` + 设置 `DASH_SCOPE_API_KEY`，然后
  `POST /api/ingest?force=true` 建首次索引，再 `POST /api/ingest` 走增量看 `documentsSkipped` 是否等于总数。

